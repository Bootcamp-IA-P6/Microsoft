"""Arrives normalize helpers.

Bronze→silver candidate building currently lives in
`pipeline.aggregate.arrives_transform.run_transform` (Phase 2 extraction).
Split further in a follow-up if needed.
"""
