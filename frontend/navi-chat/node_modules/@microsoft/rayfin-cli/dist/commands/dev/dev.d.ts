import { Dialect } from '@microsoft/rayfin-core/analysis';
import { Command } from 'commander';
import { ServiceReadinessResult } from '../../utils/docker-utils.js';
import { EnvVariable } from '../../utils/env-file-utils.js';
export interface DevConfig {
    projectRoot: string;
    projectName: string;
    composePath: string;
    healthTimeout: number;
    pollingInterval: number;
    ports: {
        webservice: number;
        sqlserver: number;
    };
    verbose: boolean;
    resetDockerComposeConfig?: boolean;
}
interface DevCommandBuildOptions {
    envFile?: string;
    processEnv?: typeof process.env;
}
/**
 * Generate environment variables for Docker Compose from rayfin.yml config and port allocation
 * @param rayfinConfig - The loaded Rayfin configuration
 * @param profiles - Enabled Docker Compose profiles
 * @param existingHealthyServiceResult - Result of existing healthy services, if any
 * @param rayfinDir - Path to rayfin directory for reading existing .env
 * @returns Array of environment variables to write to .env file
 * @internal Exported for unit testing only.
 */
export declare const generateEnvironmentVariables: (rayfinConfig: any, profiles: string[], existingHealthyServiceResult: ServiceReadinessResult | null, rayfinDir: string) => Promise<EnvVariable[]>;
export declare const applyDbConfigWithRetries: (skipDbApply: boolean, dataServiceEnabled?: boolean, dialect?: Dialect) => Promise<boolean>;
export declare const dev: (startPath?: string, buildOptions?: DevCommandBuildOptions) => Command;
export declare const devCommand: Command;
export {};
//# sourceMappingURL=dev.d.ts.map