import { Command } from 'commander';
import { generateFunctionsTypes, watchAndGenerateTypes, } from '../../utils/functions-types-generator.js';
import { findRayfinProjectRoot } from '../../utils/project-utils.js';
export const functionsTypegen = () => new Command('typegen')
    .description('Generate types.ts from function source files (or watch for changes)')
    .option('-w, --watch', 'Watch for changes and regenerate automatically')
    .action(async (options) => {
    try {
        const projectRoot = findRayfinProjectRoot(process.cwd(), {
            verbose: false,
            silent: true,
        });
        const functionsDir = `${projectRoot}/rayfin/functions`;
        await generateFunctionsTypes(functionsDir);
        console.log('✅ types.ts generated.');
        if (options.watch) {
            console.log('👀 Watching for changes...');
            // Hold a reference to the FSWatcher so V8 can't garbage-collect
            // it once the action's promise resolves. The watcher is the
            // only thing actively keeping the event loop alive — losing
            // it would silently terminate the process the moment the loop
            // drains, which is exactly the failure we hit on Windows when
            // the watcher was launched detached.
            const watcher = watchAndGenerateTypes(functionsDir);
            // Backstop: `fs.watch(..., { recursive: true })` on Windows
            // has historically been known to disengage the event loop on
            // its own (the FSWatcher is implemented via ReadDirectoryChangesW
            // and doesn't always register a libuv ref). A no-op interval
            // is the cheapest, most reliable way to pin the loop open
            // explicitly. The watcher reference is captured in this
            // closure so it can't be GC'd either.
            const heartbeat = setInterval(() => {
                // Touch the watcher so the closure-capture isn't optimized
                // away. The lookup is harmless — `fs.FSWatcher` exposes no
                // size/state we care about, we just need the reference to
                // remain reachable.
                void watcher;
            }, 60_000);
            // SIGINT/SIGTERM should close the watcher and clear the
            // heartbeat so the process exits cleanly when the parent
            // tears it down (e.g. `Stop-Process` on Windows, Ctrl+C on
            // an attached shell).
            const shutdown = () => {
                clearInterval(heartbeat);
                watcher.close();
                process.exit(0);
            };
            process.on('SIGINT', shutdown);
            process.on('SIGTERM', shutdown);
            // The CLI entry point in `scripts/main` calls `process.exit()`
            // unconditionally once `main()` resolves, which would tear
            // down the watcher the moment commander's action handler
            // settles. Return a Promise that never resolves so the outer
            // `await cli.parseAsync(...)` never finishes — the only ways
            // out from here are the SIGINT/SIGTERM shutdown above or the
            // user killing the process.
            await new Promise(() => {
                /* never resolves; the watcher + heartbeat keep the loop alive */
            });
        }
    }
    catch (error) {
        console.error('❌ Error generating types:', error instanceof Error ? error.message : error);
        process.exit(1);
    }
});
export const functionsTypegenCommand = functionsTypegen();
//# sourceMappingURL=functions-typegen.js.map