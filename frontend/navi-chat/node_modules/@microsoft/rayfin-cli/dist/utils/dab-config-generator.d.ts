import { Dialect } from '@microsoft/rayfin-core/analysis';
export interface DabGenerationOptions {
    inputDir?: string;
    distDir?: string;
    dialect?: Dialect;
    verbose?: boolean;
}
export interface DabGenerationResult {
    configPath: string;
    entities: any[];
    duration: number;
    dialect: Dialect;
}
/**
 * Generate DAB configuration from TypeScript decorators
 * @param options - Generation options
 * @returns Generation result with config path and metadata
 */
export declare function generateDabConfig(options?: DabGenerationOptions): Promise<DabGenerationResult>;
//# sourceMappingURL=dab-config-generator.d.ts.map