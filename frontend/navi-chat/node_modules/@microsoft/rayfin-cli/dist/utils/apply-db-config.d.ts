import { Dialect } from '@microsoft/rayfin-core/analysis';
import { type OutputMode } from './output-mode.js';
export declare function applyDbConfig({ remote, force, verbose, exitOnError, propagateError, dialect, remoteEndpoint, mode, }?: {
    remote?: boolean;
    force?: boolean;
    verbose?: boolean;
    exitOnError?: boolean;
    propagateError?: boolean;
    dialect?: Dialect;
    /** Pre-resolved remote endpoint URL (skips automatic resolution). */
    remoteEndpoint?: string;
    mode?: OutputMode;
}): Promise<void>;
//# sourceMappingURL=apply-db-config.d.ts.map