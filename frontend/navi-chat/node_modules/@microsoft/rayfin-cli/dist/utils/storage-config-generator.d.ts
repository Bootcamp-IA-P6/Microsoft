import type { StorageFolder } from '@microsoft/rayfin-core/analysis';
export interface StorageGenerationOptions {
    inputDir?: string;
    verbose?: boolean;
}
export interface StorageGenerationResult {
    configPath: string;
    folders: StorageFolder[];
    duration: number;
}
/**
 * Generate storage configuration from TypeScript decorators
 * @param options - Generation options
 * @returns Generation result with config path and metadata
 */
export declare function generateStorageConfig(options?: StorageGenerationOptions): Promise<StorageGenerationResult>;
//# sourceMappingURL=storage-config-generator.d.ts.map