export interface EnvironmentInfo {
    environment: string;
    source: 'custom' | 'user-git' | 'fallback';
    details: {
        username?: string;
        branch?: string;
        customValue?: string;
    };
}
/**
 * Validate environment name for Azure resource naming
 */
export declare function validateEnvironmentName(env: string): void;
/**
 * Resolve environment name from custom value or user/git context
 */
export declare function resolveEnvironment(customEnvironment?: string): Promise<EnvironmentInfo>;
/**
 * Check if environment appears to be production
 */
export declare function isProductionEnvironment(environment: string): boolean;
//# sourceMappingURL=environment-utils.d.ts.map