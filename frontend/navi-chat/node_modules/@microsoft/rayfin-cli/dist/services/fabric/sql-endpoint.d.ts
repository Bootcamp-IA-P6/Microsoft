import { FabricApiClient } from './client.js';
export interface ResolvedConnection {
    connectionString: string;
    resolvedItemId: string;
    databaseName?: string;
}
/**
 * Resolves TDS connection strings for Fabric SQL endpoints.
 * Routes to the correct Fabric API based on item type.
 */
export declare class SqlEndpointManager extends FabricApiClient {
    /**
     * Resolve the TDS connection string based on the item type
     * returned by the Get Item API.
     */
    getConnectionStringByType(workspaceId: string, itemId: string, itemType: string): Promise<ResolvedConnection>;
    private resolveLakehouse;
    private resolveSqlEndpoint;
    private resolveWarehouse;
    private resolveSqlDatabase;
}
//# sourceMappingURL=sql-endpoint.d.ts.map