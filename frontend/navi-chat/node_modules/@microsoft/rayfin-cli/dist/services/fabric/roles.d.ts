import FabricApiClient from './client.js';
export interface RoleAssignment {
    id: string;
    properties: {
        principalId: string;
        roleDefinitionId: string;
        scope: string;
        principalType: string;
    };
    [key: string]: any;
}
export interface RoleDefinition {
    id: string;
    displayName: string;
    description: string;
    [key: string]: any;
}
/**
 * Manages role assignments in Microsoft Fabric
 */
export declare class RoleManager extends FabricApiClient {
    /**
     * Lists all role definitions available in Fabric
     */
    listRoleDefinitions(): Promise<RoleDefinition[]>;
    /**
     * Gets a role definition by name (e.g., "Contributor")
     */
    getRoleDefinitionByName(roleName: string): Promise<RoleDefinition | undefined>;
    /**
     * Assigns a role to a principal (user, group, or service principal) in a workspace
     *
     * Uses the official Microsoft Fabric API endpoint:
     * `POST https://api.fabric.microsoft.com/v1/workspaces/{workspaceId}/roleAssignments`
     */
    assignRoleToWorkspace(options: {
        principalId: string;
        workspaceId: string;
        roleName: string;
        principalType?: string;
    }): Promise<RoleAssignment>;
    /**
     * Checks if a principal already has a role in a workspace
     */
    checkRoleAssignment(options: {
        principalId: string;
        workspaceId: string;
        roleName?: string;
    }): Promise<boolean>;
}
export default RoleManager;
//# sourceMappingURL=roles.d.ts.map