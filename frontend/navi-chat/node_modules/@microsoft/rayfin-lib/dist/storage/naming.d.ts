/**
 * Normalizes a storage container/folder name so it satisfies Azure Blob
 * container naming rules:
 * - lowercase letters, numbers, and hyphen (`-`) only
 * - must start and end with a letter or number
 * - no consecutive hyphens
 * - length between 3 and 63 characters
 *
 * Use this to predict the container name the SDK will target for a given
 * logical name.
 *
 * @param input - The raw container/folder name to normalize.
 * @returns A name guaranteed to satisfy Azure Blob container naming rules.
 *
 * @example
 * ```ts
 * normalizeContainerName('My Photos!'); // 'my-photos'
 * ```
 */
export declare function normalizeContainerName(input: string): string;
//# sourceMappingURL=naming.d.ts.map