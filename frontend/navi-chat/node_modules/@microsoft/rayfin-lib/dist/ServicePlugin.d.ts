import { ApiClient } from './ApiClient.js';
/**
 * (Experimental) Base class for creating custom service plugins.
 * @alpha
 * @internal
 */
export declare abstract class ServicePlugin {
    protected apiClient: ApiClient;
    constructor(apiClient: ApiClient);
}
/**
 * (Experimental) Type for a Factory method that creates a service plugin instance.
 * @alpha
 * @internal
 */
export type ServicePluginFactory<T extends ServicePlugin> = (apiClient: ApiClient) => T;
/**
 * (Experimental) Type for a mapping of service plugin factories.
 * @alpha
 * @internal
 */
export type TServiceClasses<TServices extends Record<string, ServicePlugin>> = {
    [K in keyof TServices]: ServicePluginFactory<TServices[K]>;
};
//# sourceMappingURL=ServicePlugin.d.ts.map