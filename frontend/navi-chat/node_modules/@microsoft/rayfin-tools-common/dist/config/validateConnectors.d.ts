import type { ConnectorEntry, ConnectorType } from './types.js';
/**
 * All known connector types, derived from the connector catalog.
 * Typed as `readonly ConnectorType[]` so the compiler ensures it stays
 * in sync with the `ConnectorType` union.
 */
export declare const KNOWN_CONNECTOR_TYPES: readonly ConnectorType[];
export interface ConnectorValidationError {
    sourceName: string;
    message: string;
}
/**
 * Validate all source entries from `rayfin.yml`.
 *
 * Returns an array of validation errors. An empty array means all sources are valid.
 * A missing or empty `sources` map is valid (no error).
 */
export declare function validateConnectors(sources: Record<string, ConnectorEntry> | undefined): ConnectorValidationError[];
//# sourceMappingURL=validateConnectors.d.ts.map