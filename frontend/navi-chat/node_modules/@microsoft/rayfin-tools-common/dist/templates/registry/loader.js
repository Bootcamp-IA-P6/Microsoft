import { readFile } from 'fs/promises';
import { parse as parseYaml } from 'yaml';
import { isGitUrl } from '../types.js';
/**
 * Load and parse a template-registries.yml file.
 * Returns empty registry if file doesn't exist.
 * Invalid entries are dropped with human-readable warnings.
 */
export async function loadRegistries(filePath) {
    try {
        const content = await readFile(filePath, 'utf8');
        const parsed = parseYaml(content);
        if (!parsed ||
            typeof parsed !== 'object' ||
            !Array.isArray(parsed.registries)) {
            return {
                registries: [],
                warnings: [`${filePath}: expected top-level 'registries' array`],
            };
        }
        const registries = [];
        const warnings = [];
        for (let i = 0; i < parsed.registries.length; i++) {
            const r = parsed.registries[i];
            const prefix = `${filePath}[${i}]`;
            if (!r || typeof r !== 'object') {
                warnings.push(`${prefix}: entry must be an object`);
                continue;
            }
            const rec = r;
            if (typeof rec.name !== 'string' || rec.name === '') {
                warnings.push(`${prefix}: missing or invalid 'name' field`);
                continue;
            }
            if (typeof rec.url !== 'string' || rec.url === '') {
                warnings.push(`${prefix} (${rec.name}): missing or invalid 'url' field`);
                continue;
            }
            if (!isGitUrl(rec.url)) {
                warnings.push(`${prefix} (${rec.name}): url '${rec.url}' is not a valid git URL`);
                continue;
            }
            let ref;
            if (rec.ref !== undefined) {
                if (typeof rec.ref === 'string') {
                    ref = rec.ref;
                }
                else {
                    warnings.push(`${prefix} (${rec.name}): 'ref' must be a string; got ${typeof rec.ref} ` +
                        `(${JSON.stringify(rec.ref)}). Quote the value (e.g. ref: "1.0") to preserve it. ` +
                        `Entry will be loaded without a pinned ref.`);
                }
            }
            registries.push({
                name: String(rec.name),
                displayName: String(rec.displayName || rec.name),
                description: rec.description ? String(rec.description) : undefined,
                url: String(rec.url),
                ref,
                path: typeof rec.path === 'string' ? rec.path : undefined,
                templateName: typeof rec.templateName === 'string' ? rec.templateName : undefined,
                default: typeof rec.default === 'boolean' ? rec.default : undefined,
                firstClass: typeof rec.firstClass === 'boolean' ? rec.firstClass : undefined,
            });
        }
        return { registries, warnings };
    }
    catch (err) {
        if (err.code === 'ENOENT') {
            return { registries: [], warnings: [] };
        }
        throw err;
    }
}
//# sourceMappingURL=loader.js.map