import type { TemplateRegistry } from '../types.js';
/**
 * Load and parse a template-registries.yml file.
 * Returns empty registry if file doesn't exist.
 * Invalid entries are dropped with human-readable warnings.
 */
export declare function loadRegistries(filePath: string): Promise<TemplateRegistry>;
//# sourceMappingURL=loader.d.ts.map