export interface ProcessFilesOptions {
    sourceDir: string;
    targetDir: string;
    context: Record<string, unknown>;
    ignorePatterns?: string[];
    dryRun?: boolean;
    overwrite?: boolean;
}
export interface ProcessFilesResult {
    createdFiles: string[];
    skippedFiles: string[];
}
/**
 * Replace __paramName__ placeholders in a filename with context values.
 */
export declare function renderFilename(filename: string, context: Record<string, unknown>): string;
/**
 * Recursively process template files from source to target.
 */
export declare function processFiles(options: ProcessFilesOptions): Promise<ProcessFilesResult>;
//# sourceMappingURL=file-processor.d.ts.map