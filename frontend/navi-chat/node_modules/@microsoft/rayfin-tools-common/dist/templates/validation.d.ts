import type { ProjectNames, TemplateInfo } from './types.js';
export declare const PROJECT_NAME_REGEX: RegExp;
export declare const PROJECT_SLUG_REGEX: RegExp;
/**
 * Generate a normalized slug from the provided project name.
 * Ensures we return lowercase hyphen-separated tokens without leading/trailing hyphens.
 */
export declare function generateProjectSlug(name: string): string;
/**
 * Check if a project name is valid
 */
export declare function isValidProjectName(name: string): boolean;
/**
 * Transform project name to different cases
 */
export declare function transformProjectName(name: string): ProjectNames;
/**
 * Find a template by name from the discovered templates.
 * Excludes the synthetic Copilot template from the search.
 */
export declare function findTemplateByName(templates: TemplateInfo[], name: string): TemplateInfo | undefined;
//# sourceMappingURL=validation.d.ts.map