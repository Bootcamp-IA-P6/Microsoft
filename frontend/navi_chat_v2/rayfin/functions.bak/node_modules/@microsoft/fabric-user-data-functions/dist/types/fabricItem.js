export class FabricItem {
    aliasName;
    endpoints;
    constructor(aliasName, endpoints) {
        this.aliasName = aliasName;
        this.endpoints = endpoints;
    }
    getAccessToken() {
        const first = this.endpoints.values().next();
        if (first.done) {
            throw new Error(`FabricItem '${this.aliasName}' has no endpoints with an access token.`);
        }
        return first.value.accessToken;
    }
}
//# sourceMappingURL=fabricItem.js.map