import { UserDataFunctionInvalidInputError } from "../errors/udfErrors.js";
/**
 * Attempt to convert/validate a JSON value to match the declared TypeScript type.
 * Returns the converted value on success, or a UserDataFunctionInvalidInputError on failure.
 */
export function tryConvert(value, declaredType, paramName) {
    const normalizedType = declaredType.trim();
    // If no type info / 'any' / 'unknown', pass through
    if (!normalizedType || normalizedType === "any" || normalizedType === "unknown") {
        return value;
    }
    // Handle array types: T[], Array<T>
    if (normalizedType.endsWith("[]") || normalizedType.startsWith("Array<")) {
        if (!Array.isArray(value)) {
            return new UserDataFunctionInvalidInputError(`Expected an array for parameter '${paramName}', got ${typeof value}`, { parameter_name: paramName, parameter_type: declaredType, parameter_value: String(value) });
        }
        return value;
    }
    switch (normalizedType) {
        case "string": {
            if (typeof value === "string")
                return value;
            // Convert numbers/booleans to string
            if (typeof value === "number" || typeof value === "boolean")
                return String(value);
            return new UserDataFunctionInvalidInputError(`Expected a string for parameter '${paramName}', got ${typeof value}`, { parameter_name: paramName, parameter_type: declaredType, parameter_value: String(value) });
        }
        case "number": {
            if (typeof value === "number")
                return value;
            // Convert numeric strings
            if (typeof value === "string") {
                const num = Number(value);
                if (!Number.isNaN(num))
                    return num;
            }
            return new UserDataFunctionInvalidInputError(`Expected a number for parameter '${paramName}', got ${typeof value}`, { parameter_name: paramName, parameter_type: declaredType, parameter_value: String(value) });
        }
        case "boolean": {
            if (typeof value === "boolean")
                return value; // TODO, do we need to convert "true"/"false" strings to boolean?
            return new UserDataFunctionInvalidInputError(`Expected a boolean for parameter '${paramName}', got ${typeof value}`, { parameter_name: paramName, parameter_type: declaredType, parameter_value: String(value) });
        }
        case "object":
        case "Record": {
            if (value !== null && typeof value === "object" && !Array.isArray(value))
                return value;
            return new UserDataFunctionInvalidInputError(`Expected an object for parameter '${paramName}', got ${Array.isArray(value) ? "array" : typeof value}`, { parameter_name: paramName, parameter_type: declaredType, parameter_value: String(value) });
        }
        default:
            // For unknown/custom types (interfaces, type aliases), pass through
            // as-is since we have no runtime conversion logic for them.
            return value;
    }
}
//# sourceMappingURL=addParameters.js.map